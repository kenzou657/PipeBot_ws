# PipeBot 项目开发准则

## 1. ROS2 开发规范
- **构建系统**：Python 包使用 `ament_python`，接口包使用 `ament_cmake`。
- **命名规范**：包名使用驼峰加下划线（如 `PipeBot_driver`），节点类名使用大写驼峰（如 `DriverNode`）。
- **参数管理**：所有硬编码的配置（串口号、波特率、PID参数）必须通过 ROS2 Parameter 声明。
- **日志**：使用 `self.get_logger()` 代替 `print()`，并根据严重程度选择 info/warn/error。

## 2. 跨平台串口协议 (STM32 <-> RPi)
- **协议结构**：
| 帧头 | 标识位 | 长度 | 数据位 (6 Bytes) | 校验位 | 帧尾 |
| --- | --- | --- | --- | --- | --- |
| 0x55 | 0x0* | 0x06 | Data 0 ~ Data 5 | 0x** | 0xBB |
数据为长度固定，校验位为前9个字节之和。
- **浮点处理**：串口传输一律乘以 1000 转换为整数，或使用 struct 序列化。
- **鲁棒性**：编写代码时必须考虑串口丢包或帧偏移的异常处理。

## 3. 管道检测业务逻辑
- **坐标系**：遵循 REP-105。`base_link` 在小车中心，`odom` 为里程计原点。
- **缺陷记录**：当 `PipeBot_vision` 检测到缺陷时（使用云端深度学习模型检测缺陷），必须同步获取当前的 `odom` 坐标，生成 [类型, 置信度, 里程坐标] 的元组。